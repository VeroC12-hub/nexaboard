-- Placeholder for a migration already applied to aoanslmovspmjqiqozcq before
-- this repository tracked migrations. Intentionally empty.
--
-- It exists so the CLI's migration history lines up and `db push` will apply
-- the NEXA*EDU migrations that follow. The real contents live only in the
-- remote database; recovering them needs `supabase db pull`, which requires
-- Docker. Do not delete: removing it makes every future push fail again.
select 1;
