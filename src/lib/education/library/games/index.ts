/**
 * Every named game, loaded for its side effect.
 *
 * Same pattern as the syllabus and the lessons: importing a file registers
 * what is in it, so a new game is one line here and nothing else changes.
 */

import './kg'
