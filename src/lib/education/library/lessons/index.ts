/**
 * Every stored lesson, loaded for its side effect.
 *
 * Same pattern as `library/syllabus/index.ts`: importing a file registers what
 * is in it. Nothing here is imported for a value, so a new content file is one
 * line in this list and nothing else changes.
 */

import './jhs-science'
